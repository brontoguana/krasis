# Repository rules

- Work only in this repository; network use and new dependencies are forbidden.
- `config/protected.json` is immutable.
- Preserve `notes/USER_NOTES.md`, including its pre-existing dirty line.
- Keep CLI and HTTP behavior compatible and run `npm test`.
